# Local dev: leave unset, the client will default to
#   http://<same-host-as-page>:4000
#
# Production (Netlify): set this in Netlify → Site settings →
# Environment variables, to your deployed server's public URL, e.g.
#   VITE_SERVER_URL=https://auction-room-server.onrender.com
VITE_SERVER_URL=
